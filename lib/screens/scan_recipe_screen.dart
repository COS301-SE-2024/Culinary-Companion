import 'package:flutter/material.dart';

class ScanRecipeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Scan Recipe')),
    );
  }
}
