import 'package:flutter/material.dart';

class ConfirmDetailsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Confirm Details'),
      ),
      body: Center(
        child: Text('This is the Confirm Details screen'),
      ),
    );
  }
}
